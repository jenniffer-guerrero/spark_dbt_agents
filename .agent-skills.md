---
skills:
  - dbt:
      - Creates dbt models compatible with Databricks SQL, following the silver/gold layer structure.
      - Uses macros for standardizable logic and enforces best practices for maintainability.
      - Analyzes and documents model lineage and dependencies.
  - spark:
      - Implements equivalent data logic using PySpark or Spark SQL when requested.
      - Ensures Spark code is modular, testable, and compatible with Databricks runtime.
  - incremental_mapping:
      - Designs delta-driven recomputation for REL_ID mapping using graph expansion from changed nodes only.
      - Defines and maintains state snapshots: edge_snapshot, seed_snapshot, and seed_membership.
      - Applies deterministic winner selection for competing seeds (minimum numeric seed, fallback lexical).
  - notebook_engineering:
      - Splits logic notebooks from test notebooks to keep update pipelines reusable.
      - Documents notebook flows with clear phase comments and architecture diagrams.
      - Uses JSON-based notebook code loading when nbformat is unavailable.
  - spark_runtime_reliability:
      - Uses staging-table swap writes when UPDATE/DELETE support is limited on local Parquet tables.
      - Applies checkpointing and cache invalidation to reduce lineage and stale-plan issues.
      - Handles local metastore and Java runtime constraints for stable notebook execution.
  - stateful_test_design:
      - Builds sequential mutation scenarios across shared shebang tables.
      - Validates exact inserted/removed deltas and output dedup invariants.
      - Supports baseline snapshot/restore to rerun full test sequences safely.
---
